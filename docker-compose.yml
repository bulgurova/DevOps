
services:
  web:
    build: ./back
    ports:
      - "5000:5000"
    depends_on:
      - db

  db:
    build: ./db
    ports:
      - "1433:1433"
    environment:
      SA_PASSWORD: YourStrong!Passw0rd
      ACCEPT_EULA: Y
